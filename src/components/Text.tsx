import { styled } from 'styled-components'
import { background } from './ui/variables'

const Text = styled.div`
  width: 100%;
  padding: 1rem;
  line-height: 1.5;
  background-color: ${background.body};
`

export default Text
